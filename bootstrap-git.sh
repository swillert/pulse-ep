#!/usr/bin/env bash
# bootstrap-git.sh — retired.
#
# This was a one-shot helper used to create the initial commit of
# pulse-ep from outside a sandboxed environment. It has served its
# purpose; the repository is initialised. Safe to delete.
exit 0
